# Current State — Beitco# Current State — Beitco



> **Last updated:** June 17, 2026 · branch: `dev`> ⚠️ **STALE for feature status.** This snapshot (June 2, 2026) predates the prototype build-out. The listing wizard, owner dashboard, renter hub, reviews, Q&A, messaging, matching, and sale flow are all now built in the prototype (frontend + localStorage). **For the accurate, current feature inventory and gaps, read `.ai/PRD.md` §5–§8.** This file is kept for the backend/infra notes and historical context.

> Honest snapshot of what's actually built. For the product vision/scope read `.ai/PRD.md`; for the backend work plan read `.ai/BACKEND_TASKS.md`.

> Last updated: June 2, 2026

---> What's actually built vs planned. Be honest in this doc.



## TL;DR---



- **Frontend: feature-complete prototype** on a localStorage mock. Trust-first bed-level marketplace — listing wizard, search, property detail, owner dashboard, renter hub, reviews + Q&A, messaging, viewing/booking requests, matching, sale + nightly, KYC verification, **moderation queue**, public profiles, maps, dark mode, PWA. Real **computed** trust engine (T-1→T-5) + matching engine, both unit-tested (31 tests). Typechecks clean, production build healthy.## ✅ What Works Today

- **Backend: NestJS exists and is partially built, but against a PRE-PIVOT schema.** The repo was originally a social/video platform; the housing pivot left the API half-aligned. On `dev` we **removed the social/video modules** (posts, comments, social, likes, feed, groups, videos). What remains is housing-relevant but needs the schema rewritten to the bed-level + trust model and the modules adapted to it.

- **Not yet connected:** the frontend still reads from localStorage; nothing talks to the API. Wiring them is the whole point of the next phase (`.ai/BACKEND_TASKS.md`).### Frontend (`apps/web/`)

- TanStack Start (Vite + React 19) running on `pnpm web` → http://localhost:8080

---- Tailwind v4 + shadcn/ui design system

- Full Arabic RTL with Egyptian colloquial dialect

## Git & Branches (set up June 17)- **Home page** (`/`): hero, search bar (UI only), filter chips (UI only), grid of 6 hardcoded properties

- **Property detail page** (`/property/$id`): gallery, trust section, quality scores, amenities, bed-occupancy grid, reviews, Q&A, sticky pricing sidebar with cost breakdown

| Branch | Purpose |- Trust UI components: `TrustBadge`, `ScoreBar`

|---|---|- Site chrome: `SiteHeader`, `SiteFooter` (Egyptian Arabic)

| `old` | Full snapshot of everything before cleanup — social/video API modules, `web-old`, `frontend-candidates`, legacy `docs/`. Nothing is lost; restore from here if ever needed. |- Routes auto-generated by TanStack Router plugin

| `main` | Production branch (currently = initial snapshot; promote from `dev` when ready). |

| `dev` | **Active.** Cleaned for backend work — social/video modules removed, cruft deleted, docs updated. All backend work happens here. |### Backend (`apps/api/`)

- NestJS 11 scaffolded

---- Prisma + Postgres + PostGIS configured in `docker-compose.yml`

- Redis + Meilisearch declared but flaky in dev (Redis connection errors)

## ✅ Frontend — `apps/web/` (`tanstack_start_ts`)- **Not connected to frontend yet**



**Stack:** TanStack Start (Vite + React 19), TypeScript strict, Tailwind v4, shadcn/ui, Arabic RTL (Egyptian dialect), Leaflet (maps), Vitest. Runs on `pnpm web` → http://localhost:8080.### Monorepo

- Turborepo + pnpm workspace

**Built (all on a localStorage mock — `lib/beitco/store.ts` + `seed-data.ts`):**- Root scripts: `pnpm web`, `pnpm api`, `pnpm dev` (all)

- **Discovery:** home, search (filters: type, purpose rent/sale, gender, area, price, free-beds, verified, **nightly**), saved searches + alerts UI, saved listings.- `.github/copilot-instructions.md` and `.cursorrules` present

- **Property detail:** gallery w/ accessible lightbox, computed trust breakdown popover, quality scores, amenities, bed/room occupancy, reviews (sort + helpful + owner reply), Q&A, **Google-Maps location**, request viewing / bed-level booking, message owner, share.

- **Listing wizard** (`/list/new`, 8 steps): apartment → rooms → beds model, sale + nightly pricing, gender policy, amenities, photos, **interactive map pin-drop (Leaflet + GPS)**, review. Moderation-gated publish.---

- **Owner dashboard:** listings, leads (viewing/booking requests + renter reputation), reviews, analytics (views/saves/funnel), response-rate card, KYC verification (ID + selfie), **admin moderation queue** (`/dashboard/moderation`).

- **Renter hub (`/me`):** role-aware overview, matches (explainable scoring), applications, tenancies, saved, saved searches, preferences, settings (theme, notifications), public profiles (`/u/$id`).## 🟡 Partially Built

- **Trust system (real, computed — `lib/beitco/trust.ts`):** T-1 listing/owner scores (Bayesian-smoothed, verification cap), T-2 quality-from-reviews, T-3 response-rate from message behavior, T-4 two-sided renter reputation, T-5 transparency UI.

- **Matching engine (`lib/beitco/matching.ts`):** weighted, explainable (reasons/misses), gender hard-constraint.| Thing | Status |

- **Platform:** mobile bottom-tab nav, installable PWA (manifest), dark mode (FOUC-safe), global toasts, skip-link + reduced-motion + focus-trapped dialogs, dynamic per-listing SEO/OG, favicon.|---|---|

| Listing data | Hardcoded mock array in `index.tsx` + `lib/beitco/properties.ts` (2 sample properties) |

**Quality gates:** `pnpm --filter tanstack_start_ts test` → 31 passing (trust + matching engines). `npx tsc --noEmit` clean. `vite build` clean (~1s), Leaflet code-split.| Search/filter | UI present, no logic |

| Trust score | Displayed, hardcoded values, no calculation |

---| Reviews | Display only, can't post |

| Q&A | Display only, can't post |

## 🟡 Backend — `apps/api/` (`@beitco/api`)| Auth | Not started |

| API endpoints | Module scaffolds exist, no working endpoints |

**Stack:** NestJS 11, Prisma + PostgreSQL (+PostGIS), Redis, Meilisearch, JWT + Passport, Socket.io (chat), Swagger, Throttler. Global guards: Throttler → JwtAuth → Tier.

---

**Modules present after the `dev` cleanup** (LOC = how built-out, but **against the pre-pivot schema**):

## 🔲 Not Built

| Module | LOC | State / what it needs |

|---|---|---|- Phone OTP authentication

| `auth` | ~849 | OTP send/verify, refresh, google, apple, logout, JWT, tier guard. **Keep; adapt to housing user/tier model.** |- "حط شقتك" (post a listing) flow

| `listings` | ~830 | Full CRUD + saved + similar. **Built against the old flat `Listing` model — needs rework to apartment→rooms→beds.** |- Owner dashboard

| `admin` | ~948 | Reports, user tiers, suspend. **Keep; trim social report types.** |- Renter dashboard / saved listings

| `moderation` | ~823 | Approval/report queue. **Keep; align to listing approval (MOD-1).** |- Real DB-backed listings

| `chat` | ~664 | Conversations + messages + gateway. **Keep for owner↔renter messaging.** |- Search via Meilisearch

| `search` | ~668 | Meilisearch indexing. **Keep; reindex bed-level listings.** |- Messaging between owner & renter

| `analytics` | ~538 | Event tracking. **Keep; trim to listing analytics.** |- Viewing-request workflow

| `users` | ~509 | Profile CRUD. **Keep; align to housing profile + verification.** |- Review posting (with 30-day-resident gate)

| `notifications` | ~418 | Notification model + delivery. **Keep.** |- Trust score calculation engine

| `payments` | ~34 | Stub only. **Build: subscription + success fee.** |- Image upload (S3/R2)

| `health`, `common`, `prisma` | — | Infra. Keep. |- Video tours (Mux)

- Mobile shell (Capacitor)

**Removed on `dev`** (preserved in `old`): `posts`, `comments`, `social`, `likes`, `feed`, `groups`, `videos` — social-network/video-platform features irrelevant to a housing marketplace.- Admin moderation panel

- Email/SMS notifications

**Prisma schema (`apps/api/prisma/schema.prisma`):** still the **pre-pivot social schema** (Post, Comment, Like, Follow, Group, Video, Hashtag, PollVote… alongside Listing/Review/SavedListing). It still has a valid `Listing` model so the API compiles, but it does **not** model bed-level inventory or the computed trust system. **Rewriting it is backend task B-0.**- Payments



**Status:** `npx tsc --noEmit` passes after cleanup (Prisma client regenerated). **Not connected to the frontend.** Redis/Meilisearch need `docker compose up -d`.---



---## 📂 Repo Layout (current)



## 🔲 Not built / next phase (see `.ai/BACKEND_TASKS.md`)```

Beitco/

- Bed-level + trust Prisma schema (the foundation).├── apps/

- Real listings API consumed by the frontend (replace localStorage with TanStack Query).│   ├── web/              ← TanStack Start frontend (LIVE)

- Real phone OTP + JWT session wired into `auth.tsx`.│   ├── web-old/          ← old Next.js attempt, archived, ignore

- Persisting writes: listings, reviews, Q&A, messages, leads, tenancies, saved, saved-searches.│   └── api/              ← NestJS backend (scaffold only)

- Porting the **trust** + **matching** engines to NestJS services.├── packages/             ← shared types/utils (mostly empty)

- Meilisearch-backed search; media to object storage; payments; SMS/push notifications.├── frontend-candidates/  ← design exploration, ignore

- Mobile shell (Capacitor) — deferred.├── docs/                 ← legacy long-form docs (need rewrite)

├── .ai/                  ← strategy docs (this folder = source of truth)

---├── docker-compose.yml    ← Postgres + PostGIS + Redis + Meilisearch

└── package.json          ← root scripts

## 📂 Repo Layout (after `dev` cleanup)```



```---

Beitco/

├── apps/## ⚠️ Known Issues / Tech Debt

│   ├── web/   ← TanStack Start frontend — feature-complete prototype (mock data)

│   └── api/   ← NestJS backend — housing-relevant modules, pre-pivot schema1. **`apps/api`** has Redis/Meilisearch errors on startup — probably needs `docker compose up -d` first.

├── packages/  ← shared @beitco/types, @beitco/utils, @beitco/validators2. **`.ai/ARCHITECTURE.md`, `.ai/DB_SCHEMA.md`, `.ai/API_SPEC.md`** were written before the bed-level pivot and need rewriting against the new model.

├── .ai/       ← strategy + state docs (source of truth)3. **`docs/`** folder has older long-form planning docs that are now outdated; the `.ai/` folder supersedes them.

├── docker-compose.yml  ← Postgres+PostGIS, Redis, Meilisearch4. **`apps/web-old`** should be deleted once we're confident the new frontend covers everything.

└── package.json        ← root scripts (pnpm web / api / dev)5. **`frontend-candidates/`** can be deleted — design pick is final.

```

---

---

## 🎯 Next Concrete Steps (priority order)

## ⚠️ Known Issues / Tech Debt

1. **Wire frontend ↔ API** — replace `lib/beitco/properties.ts` with `GET /api/v1/properties` from NestJS.

1. **Prisma schema is pre-pivot** — the single biggest backend item (B-0).2. **Phone OTP auth** — Egyptian numbers only at first.

2. **`.ai/ARCHITECTURE.md`, `DB_SCHEMA.md`, `API_SPEC.md`** describe the old social model — flagged stale at the top of each; rewrite as the schema/API land.3. **"حط شقتك" flow** — owner can post an apartment with bed count.

3. **`payments` module is a stub.**4. **Search & filter** — Meilisearch index + frontend wiring.

4. **Redis/Meilisearch** flaky unless `docker compose up -d` runs first.5. **Update `.ai/DB_SCHEMA.md`** to reflect bed-level inventory model.

5. **Frontend component tests** — engines are covered (31 tests), UI components are not (needs jsdom).6. **Update `.ai/API_SPEC.md`** to reflect new endpoints.

